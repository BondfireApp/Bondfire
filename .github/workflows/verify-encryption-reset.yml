name: Verify encryption reset

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/verify-encryption-reset.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.10.0
          cache: npm
      - name: Check reset source syntax
        run: |
          node --check functions/api/_lib/privateReset.js
          node --check functions/api/orgs/'[orgId]'/privacy/reset.js
          node --check src/lib/privateEncryptionReset.js
      - name: Run encryption reset regression
        run: node scripts/private-encryption-reset-regression.mjs
      - name: Install dependencies
        run: npm ci
      - name: Run product reliability checks
        run: npm run test:product-reliability
      - name: Build production bundle
        run: npm run build
